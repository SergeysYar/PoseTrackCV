# Contributing

1. Create a virtual environment and install dependencies.
2. Run `pytest` before opening PRs.
3. Keep modules typed, documented, and testable.
4. Use config files instead of hardcoded paths.

