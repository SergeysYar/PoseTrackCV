# Dataset Preparation
1. Extract frames from top-view videos.
2. Annotate bounding boxes (optional angle label).
3. Convert labels to YOLO text format.
4. Run validation and split train/val/test.
5. Generate `dataset_stats.csv` and `dataset_summary.md`.

